$(function() {
	// 模态框设置
	$("#internal-add").draggable();// 为模态对话框添加拖拽
	$("#internal-add").css("overflow", "hidden");// 禁止模态对话框的半透明背景滚动
	
	$("#delete-internal").draggable();// 为模态对话框添加拖拽
	$("#delete-internal").css("overflow", "hidden");// 禁止模态对话框的半透明背景滚动
	
	$("#edit-internal").draggable();// 为模态对话框添加拖拽
	$("#edit-internal").css("overflow", "hidden");// 禁止模态对话框的半透明背景滚动
});
