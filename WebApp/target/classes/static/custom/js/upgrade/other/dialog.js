$(function() {
	// 模态框设置
	$("#external-add").draggable();// 为模态对话框添加拖拽
	$("#external-add").css("overflow", "hidden");// 禁止模态对话框的半透明背景滚动
	
	$("#delete-external").draggable();// 为模态对话框添加拖拽
	$("#delete-external").css("overflow", "hidden");// 禁止模态对话框的半透明背景滚动
	
	$("#edit-external").draggable();// 为模态对话框添加拖拽
	$("#edit-external").css("overflow", "hidden");// 禁止模态对话框的半透明背景滚动
});
